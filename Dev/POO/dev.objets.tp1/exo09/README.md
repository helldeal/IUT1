Implémentez les fonctions `somme(tab : Array<Int>)`, `recherche(tab : Array<Int>, v :Int)`, 
`estEnsemble(tab : Array<Int>)` et `envers(tab : Array<Int>)`
et validez votre code en passant les cas de tests fournis
